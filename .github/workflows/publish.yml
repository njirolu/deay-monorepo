name: Publish to npm

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Build all packages
        run: bun run build:all

      - name: Setup npmrc
        run: echo "//registry.npmjs.org/:_authToken=${{ secrets.NPM_TOKEN }}" > .npmrc

      - name: Copy package.json for @deay/ui
        run: cp packages/ui/package.json packages/ui/dist/

      - name: Copy .npmrc to dist
        run: cp .npmrc packages/ui/dist/

      - name: Publish @deay/ui
        run: |
          cd packages/ui/dist
          bun publish --access public

      - name: Copy package.json for @deay/mcp
        run: |
          cp packages/mcp-server/package.json packages/mcp-server/dist/
          # Update files field to point to current directory
          sed -i 's/"files": \["dist\/", "README.md"\]/"files": [".\/", "README.md"]/g' packages/mcp-server/dist/package.json

      - name: Copy .npmrc to mcp dist
        run: cp .npmrc packages/mcp-server/dist/

      - name: Publish @deay/mcp
        run: |
          cd packages/mcp-server/dist
          bun publish --access public
